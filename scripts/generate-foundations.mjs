import { writeFile } from 'node:fs/promises';
import { join, resolve } from 'node:path';

const lessons = {
  'evaluation-metrics': {
    title:'Evaluation Metrics', badge:'Model Evaluation · Foundations', intro:'Choose metrics from the real consequences of errors—not from whichever score looks healthiest.', outcomes:['Match metrics to decisions','Separate discrimination from calibration','Report uncertainty'], prerequisite:'Basic prediction and train/validation/test splitting', next:'Imbalanced Classification',
    problem:'A model produces many kinds of errors. A metric turns those errors into a comparison, but only after the real decision defines what matters. A hospital triage model and a spam filter should not inherit the same priorities by convenience.',
    intuition:'MAE counts absolute regression misses in outcome units; RMSE gives large misses extra weight. Accuracy counts correct labels, precision asks how many predicted positives were right, and recall asks how many actual positives were found. Log loss and Brier score evaluate probabilities, while calibration compares predicted risks with observed frequencies.',
    worked:'A screening model sees 1,000 people: 100 have disease. It finds 80 of them and falsely flags 90 healthy people. TP=80, FN=20, FP=90, TN=810. Accuracy is 89%, recall is 80%, and precision is 47%. The apparently healthy accuracy hides that more than half of alerts are false.',
    interactive:'Move the decision threshold. Watch the confusion matrix, precision, recall and expected error cost change together.',
    assumptions:'The evaluation sample must represent the deployment population, labels must be trustworthy, and each metric must be calculated on genuinely held-out predictions.', failures:'Accuracy fails under imbalance or unequal costs. MAPE behaves badly near zero. ROC-AUC can obscure performance at the operating region, and a strong ranking score does not guarantee calibrated probabilities.',
    workflow:['Define the decision and error costs','Choose primary and secondary metrics before model comparison','Tune model and threshold on validation data','Lock the full pipeline','Report final test metrics with bootstrap or repeated-sample intervals'],
    report:'State the metric definition, threshold, prevalence or outcome scale, baseline, point estimate, uncertainty interval and real-world costs.', misuse:'Selecting whichever metric makes a preferred model look best after seeing the results.',
    questions:[['False negatives are extremely costly. Which metric deserves priority?',['Recall','Specificity','Raw accuracy'],0,'Recall measures the fraction of actual positives found; the cost requirement makes missed positives central.'],['Why can 99% accuracy be useless?',['The positive class may be only 1% and never detected','Accuracy always ignores true negatives','Accuracy is a probability score'],0,'A classifier predicting every case negative can be 99% accurate and have zero recall.'],['Which quantity evaluates probability calibration?',['Brier score','F1 score','Specificity'],0,'Brier score measures squared probability error and is sensitive to calibration.'],['What belongs on the final test set?',['One evaluation of the locked model and threshold','Threshold search across 100 cutoffs','Feature selection before fitting'],0,'All choices must be locked before the test evaluation.'],['What should accompany a metric?',['Uncertainty and a relevant baseline','Only the largest decimal precision','Training accuracy'],0,'A point estimate needs context, comparison and sampling uncertainty.']], interaction:'metrics'
  },
  'data-leakage-pipelines': {
    title:'Data Leakage & Pipelines', badge:'Model Evaluation · Workflow', intro:'Keep validation, test, group, and future information out of model development.', outcomes:['Recognise leakage','Build fold-safe pipelines','Choose valid resampling'], prerequisite:'Basic supervised learning', next:'Evaluation Metrics or Model Selection',
    problem:'A model can appear excellent because information crossed the boundary between development and evaluation. Leakage estimates performance for a procedure that cannot exist in deployment.', intuition:'Treat the pipeline—not only the final algorithm—as the model. Anything that learns a mean, category frequency, component, selected feature, threshold, or synthetic observation must learn it inside each training fold.',
    worked:'Suppose missing ages are filled with the mean of all 1,000 rows and then five-fold CV is run. Every validation fold has influenced its own imputed values. The honest workflow fits five separate imputers, each on that fold’s 800 training rows, and applies it to the 200 held-out rows.',
    interactive:'Classify each pipeline as safe or leaking. Feedback identifies exactly where information crosses the boundary.', assumptions:'The split must match the unit of independence and deployment timing. Features must exist at the moment a prediction is made.', failures:'Random CV fails for time order, repeated patients, households, organisations, duplicated rows, and target-derived features. Oversampling before CV creates near-duplicates across folds.',
    workflow:['Define prediction time and independent unit','Reserve the final test set','Choose stratified, grouped, time-series, or nested resampling','Fit every learned transform inside training folds','Lock and evaluate once'], report:'Document split units, dates or groups, pipeline order, cross-validation variant, test isolation and every target-aware transformation.', misuse:'Preprocessing the full dataset before splitting because “it is not the model.”',
    questions:[['Which workflow leaks?',['Scale all rows, then cross-validate','Fit scaling inside each training fold','Apply a training-fitted scaler to validation rows'],0,'Scaling all rows lets each validation fold influence its own transformation.'],['Where should oversampling happen?',['Inside each training fold','Before creating folds','On the final test set'],0,'Oversampling must not create synthetic or duplicated information across held-out boundaries.'],['Repeated visits from each patient require…',['Grouped CV by patient','Ordinary random row CV','Leaving visits unsplit'],0,'All visits from one patient belong in the same fold.'],['For forecasting, use…',['Time-ordered validation','Random shuffling','A test set from the middle of training'],0,'Every validation observation must occur after the observations used to fit it.'],['What is target leakage?',['Using information unavailable at prediction time that reveals the outcome','Having many predictors','Using a categorical feature'],0,'Availability at the real prediction moment is the key test.']], interaction:'pipeline'
  },
  'regression-diagnostics': {
    title:'Regression Diagnostics', badge:'Regression · Foundations', intro:'Decide when coefficient, interval, and prediction claims deserve trust.', outcomes:['Read residual plots','Diagnose influence and collinearity','Distinguish interval types'], prerequisite:'Simple and Multiple Linear Regression', next:'Polynomial Regression or Model Selection',
    problem:'A fitted equation always returns numbers; diagnostics ask whether the functional form, error behaviour, and individual observations make those numbers defensible.', intuition:'Residuals should fluctuate without systematic shape around zero. Curvature signals missing functional form, a funnel signals changing variance, a Q–Q departure challenges normal-error inference, and an isolated high-leverage point can rotate the fitted line.',
    worked:'A salary model has residual spread of ±5k for junior staff and ±40k for senior staff. The fan pattern violates constant variance. Coefficients may still describe a mean pattern, but ordinary standard errors and a single-width prediction band are unreliable; robust errors or a variance model may be needed.',
    interactive:'Switch between healthy, curved, funnel-shaped, and influential residual patterns. The plot and recommended response update together.', assumptions:'Correct functional form, independent errors, stable conditional variance, and residual normality when classical small-sample inference needs it.', failures:'Outliers, high leverage, extrapolation, omitted nonlinearities, dependence and multicollinearity can destabilise estimates or uncertainty.',
    workflow:['Plot outcome and predictors','Fit the pre-specified model','Inspect residual–fitted and Q–Q plots','Check leverage, Cook’s distance and VIF','Run justified sensitivity analyses','Validate predictions on unseen data'], report:'Report coefficients with confidence intervals, residual standard error, diagnostic findings, influential-case sensitivity, validation error, and prediction intervals.', misuse:'Deleting observations solely because their removal produces significance.',
    questions:[['A widening residual funnel indicates…',['Heteroscedasticity','Perfect normality','Low leverage'],0,'Residual variance changes with the fitted value.'],['Leverage differs from influence because…',['Leverage describes unusual predictor position; influence describes effect on the fit','They are identical','Influence ignores residual size'],0,'Influence combines position and discrepancy to measure how much the fit changes.'],['For one future observation use…',['A prediction interval','A confidence interval for the mean','Only R²'],0,'Prediction intervals include mean-estimation uncertainty and individual noise.'],['A curved residual pattern suggests…',['Missing functional form','Guaranteed causality','Too many test rows'],0,'The mean relationship has not been represented adequately.'],['A high R² proves…',['None of correctness, causality, or transportability','The model is causal','Residual variance is constant'],0,'R² alone cannot validate the model.']], interaction:'diagnostics'
  },
  'probability-sampling': {
    title:'Probability & Sampling Distributions', badge:'Statistics · Foundations', intro:'Connect random variables, distributions, samples, and standard errors.', outcomes:['Model uncertainty','Understand repeated sampling','Interpret standard error'], prerequisite:'None', next:'Confidence Intervals & Hypothesis Testing',
    problem:'Observed statistics vary from sample to sample. Probability provides a language for that uncertainty and sampling distributions describe how an estimator behaves under repeated study designs.', intuition:'A population distribution describes individual values. A sampling distribution describes a statistic—such as a mean—across repeated samples. Its standard deviation is the standard error.',
    worked:'If individual scores have standard deviation 12, the mean of n=36 independent observations has standard error about 12/√36=2. Doubling n does not halve uncertainty; roughly quadrupling n does.',
    interactive:'Choose sample size and draw repeated samples. The histogram of sample means narrows as independent information increases.', assumptions:'Repeated-sampling claims presume the same population and design. The usual √n improvement requires independent or appropriately modelled observations.', failures:'Density height is not point probability; dependence reduces effective sample size; skew and heavy tails can slow normal approximations; convenience samples do not become representative by growing.',
    workflow:['Define population and random variable','Identify discrete or continuous support','Specify the sampling design','Choose a statistic','Study or approximate its sampling distribution','Quantify standard error'], report:'Name the population, sample mechanism, statistic, sample size, dependence structure, estimated standard error and approximation used.', misuse:'Confusing the spread of individual observations with the uncertainty of their mean.',
    questions:[['A sampling distribution describes…',['A statistic across repeated samples','Individuals in one sample','Only the population density'],0,'Its random object is the statistic produced by the sampling procedure.'],['Why does standard error often shrink with n?',['Independent observations add information and averaging reduces noise','The population variance disappears','Large samples eliminate bias automatically'],0,'For a mean under independence, SE scales approximately as 1/√n.'],['Is a continuous density height a probability?',['No; probability is area over an interval','Yes, always','Only when above one'],0,'A point has zero probability in a continuous model; interval area is probability.'],['If observations are clustered, naive SE is often…',['Too small','Always too large','Unaffected'],0,'Within-cluster similarity means less independent information than the row count suggests.'],['To halve a mean’s SE, n must be approximately…',['Four times as large','Twice as large','Half as large'],0,'Because SE is proportional to 1/√n, halving it requires quadrupling n.']], interaction:'sampling'
  },
  'confidence-hypothesis-testing': {
    title:'Confidence Intervals & Hypothesis Testing', badge:'Statistics · Inference', intro:'Use compatible intervals, tests, effect sizes, and decision rules without overclaiming.', outcomes:['Interpret p-values correctly','Read confidence intervals','Control multiplicity'], prerequisite:'Probability & Sampling Distributions', next:'ANOVA, Correlation, or Chi-Square Test',
    problem:'Inference connects a noisy sample to a population quantity. Confidence intervals show compatible effect sizes; tests quantify how surprising the data are under a specified null model.', intuition:'A p-value is computed assuming the null and asks about results at least as extreme. A confidence procedure is calibrated across repeated samples. Neither replaces effect size, assumptions, or practical meaning.',
    worked:'An estimated mean difference is 4 points with a 95% CI from 1 to 7 and p=.01. The data are inconsistent with zero under the test assumptions, while plausible effects range from small to substantial. The p-value is not a 1% probability that the null is true.',
    interactive:'Change sample size and effect size. See the standard error, confidence interval and approximate p-value change—without changing what a p-value means.', assumptions:'The test, estimator, sampling design and dependence model must match. Pre-specified hypotheses protect the advertised error rate.', failures:'Optional stopping, many uncorrected tests, model misspecification, low power and selective reporting invalidate simple interpretations.',
    workflow:['Define estimand, H₀ and H₁','Choose α and analysis before results','Check design and assumptions','Compute estimate, effect size, interval and p-value','Adjust for multiplicity when required','Interpret practical significance'], report:'Report estimate and compatible interval, exact p-value where useful, effect size, assumptions, multiplicity plan, sample size and practical consequences.', misuse:'Calling p>.05 “no effect” or p<.05 “proof.”',
    questions:[['A 95% confidence procedure means…',['Across repetitions, 95% of intervals capture the parameter under assumptions','There is always a 95% posterior probability inside this interval','95% of observations fall inside'],0,'Frequentist coverage is a long-run property of the procedure.'],['A p-value is not…',['The probability the null is true','A tail probability under the null','Affected by sample size'],0,'The null is conditioned on when calculating the p-value.'],['Testing 20 outcomes requires attention to…',['Multiplicity','Feature scaling only','Class prevalence'],0,'Multiple opportunities for small p-values inflate false positives.'],['Failing to reject means…',['Evidence was insufficient at the chosen rule','The null is proven','The effect is exactly zero'],0,'Absence of sufficient evidence is not evidence of exact absence.'],['What completes an inferential result?',['Effect size, interval and practical interpretation','Only stars denoting significance','Only the sample mean'],0,'Magnitude and uncertainty are required for useful interpretation.']], interaction:'testing'
  },
  'random-forest': {
    title:'Random Forest', badge:'Machine Learning · Ensembles', intro:'Average decorrelated decision trees to improve prediction stability.', outcomes:['Understand bagging','Tune without leakage','Interpret cautiously'], prerequisite:'Classification Trees and Regression Trees', next:'Gradient Boosting',
    problem:'A single tree is interpretable but unstable. Random forests average many deliberately varied trees so individual split noise cancels while shared predictive structure remains.', intuition:'Each tree sees a bootstrap sample and a random subset of candidate features at each split. Averaging mainly reduces variance when trees make partly independent errors.',
    worked:'One tree classifies 82% of validation cases, but across resamples it ranges from 74% to 87%. A 300-tree forest reaches 86% with a much narrower range. The gain comes from averaging; 300 trees are not 300 independent datasets.',
    interactive:'Adjust tree count and features considered per split. Watch out-of-bag error stabilise and tree correlation change.', assumptions:'Training data must represent deployment, observations must respect split structure, and features must be available at prediction time.', failures:'Forests extrapolate poorly in regression, probabilities may need calibration, correlated predictors distort importance, and large forests cost memory and latency.',
    workflow:['Build a simple tree baseline','Place preprocessing inside resampling','Tune feature sampling and leaf constraints','Grow enough trees for stable error','Check calibration and subgroup performance','Evaluate the locked forest once'], report:'Report tree count, feature sampling, depth/leaf constraints, seed, resampling, OOB use, final metrics with uncertainty, calibration and importance method.', misuse:'Treating impurity importance as a causal ranking of variables.',
    questions:[['Bagging mainly reduces…',['Variance','Irreducible noise','Outcome prevalence'],0,'Averaging unstable learners reduces variance when errors are not perfectly correlated.'],['Random feature subsets help by…',['Decorrelating trees','Making every tree identical','Guaranteeing causal importance'],0,'Less-correlated errors average away more effectively.'],['OOB error can be misused when…',['Extensive tuning repeatedly optimises it without outer evaluation','No bootstrapping occurs','There are many trees'],0,'Repeated optimisation makes the estimate part of model selection.'],['Permutation importance means…',['Predictive reliance under a perturbation, not causality','The feature causes the outcome','The coefficient is stable'],0,'Association, correlation and feature dependence shape importance.'],['A forest regression prediction outside the training range is…',['Usually poor at extrapolating trends','Guaranteed linear','Always unbiased'],0,'Tree averages are built from observed leaf outcomes.']], interaction:'forest'
  },
  'gradient-boosting': {
    title:'Gradient Boosting', badge:'Machine Learning · Ensembles', intro:'Build an additive model by repeatedly correcting current prediction errors.', outcomes:['Understand boosting','Regularise complexity','Validate early stopping'], prerequisite:'Decision Trees and Evaluation Metrics', next:'Probability Calibration or Interpretability',
    problem:'Instead of averaging independent trees, boosting builds a sequence in which each new weak learner targets the current model’s loss gradient.', intuition:'Learning rate controls how much each tree contributes; tree count controls how many correction steps occur. Small learning rates often need more trees, so these settings must be tuned together.',
    worked:'At learning rate .3, validation loss falls quickly then rises after 60 trees. At .05 it improves more slowly and reaches a better minimum near 300 trees. Early stopping chooses the round from validation data, not the test set.',
    interactive:'Move learning rate and tree count. Compare stylised training and validation loss and identify underfitting versus overfitting.', assumptions:'The loss must match the target and decision; validation must reflect deployment; preprocessing and early stopping must stay inside development data.', failures:'Too many rounds chase noise, poor tuning overfits, probabilities may be miscalibrated, and importance is unstable under correlated predictors.',
    workflow:['Choose loss and baseline','Create a leakage-safe validation scheme','Tune learning rate, complexity and subsampling jointly','Use validation-based early stopping','Assess calibration and subgroup error','Evaluate the locked model once'], report:'Report implementation, loss, learning rate, selected round, tree constraints, subsampling, early-stopping rule, resampling, metrics, calibration and uncertainty.', misuse:'Choosing the number of trees from the final test-loss minimum.',
    questions:[['Lower learning rate usually needs…',['More boosting rounds','No trees','A higher test threshold'],0,'Each learner contributes less, so more steps are needed.'],['Early stopping should use…',['Validation data','Final test data','Training loss alone'],0,'It is a hyperparameter choice and belongs in development.'],['Training loss falls while validation rises means…',['Overfitting','Calibration is perfect','Variance is zero'],0,'The model is fitting training-specific detail.'],['For probabilities also inspect…',['Brier score and calibration','Only accuracy','Only tree depth'],0,'Ranking and hard-label accuracy do not guarantee probability quality.'],['Boosting importance proves…',['Neither causality nor unique importance','A causal mechanism','No feature correlation'],0,'It is a model-dependent predictive summary.']], interaction:'boosting'
  },
  'missing-data-encoding': {
    title:'Missing Data & Encoding', badge:'Data Preparation · Workflow', intro:'Treat missingness and categories as modelling choices, not housekeeping.', outcomes:['Diagnose missingness','Encode categories safely','Prevent preprocessing leakage'], prerequisite:'Basic data types and train/validation/test splitting', next:'Data Leakage & Pipelines',
    problem:'Missing values and categories carry structure. A convenient fill value or numeric code can change the estimand, introduce false order, or leak outcome information.', intuition:'Imputation predicts or substitutes missing values; encoding represents categories numerically. Both are learned transformations and belong inside the pipeline.',
    worked:'Income is missing more often for high earners. Mean imputation pulls their values toward the centre and understates variation. A missingness indicator may help prediction, but it does not make the missing-at-random assumption true for inference.',
    interactive:'Change missingness rate and strategy. Compare remaining bias, predictive error and whether an encoding introduces artificial order.', assumptions:'Missing-data methods rely on assumptions about why values are absent. Category encodings assume a structure—unordered, ordinal, or target-related—that must be defensible.', failures:'Single imputation understates inferential uncertainty; ordinal codes create fake distances; target encoding leaks without cross-fitting; unseen categories can break deployment.',
    workflow:['Profile missingness and category levels','Separate structural from accidental missingness','Split data before fitting transforms','Choose imputation for the analysis goal','Encode with explicit reference and unknown-level policy','Run sensitivity analyses'], report:'Report missingness rates, mechanism assumptions, imputation model, number of imputations, encoding, reference levels, unknown-category policy and sensitivity.', misuse:'Using zero as “missing” when zero has a real scientific meaning.',
    questions:[['Target encoding should be…',['Cross-fitted inside training data','Computed once on all rows','Computed from the test labels'],0,'Each held-out row must not influence its own encoded target mean.'],['Single imputation for inference often…',['Understates uncertainty','Guarantees unbiased coefficients','Creates multiple datasets'],0,'Treating filled values as known ignores imputation uncertainty.'],['Integer codes for unordered colours create…',['Artificial order and distance','Perfect one-hot encoding','No modelling assumption'],0,'Codes such as red=1, blue=2, green=3 imply geometry that may not exist.'],['An unseen category policy belongs…',['In the fitted pipeline','After test scoring fails','Only in documentation'],0,'Deployment must define how new levels are represented.'],['A missingness indicator…',['May help prediction but does not prove a missingness mechanism','Makes data MCAR','Restores every true value'],0,'It is a feature, not a proof about why data are absent.']], interaction:'missing'
  },
  'imbalanced-classification': {
    title:'Imbalanced Classification', badge:'Machine Learning · Classification', intro:'Evaluate rare outcomes using realistic prevalence, costs, and calibrated probabilities.', outcomes:['Reject accuracy traps','Tune thresholds by cost','Resample safely'], prerequisite:'Logistic Regression and Evaluation Metrics', next:'Probability Calibration or Fairness',
    problem:'When positives are rare, a model can be highly accurate while missing every important case. Evaluation must reflect prevalence, asymmetric costs, and operational capacity.', intuition:'The threshold converts probabilities to actions. Lower thresholds increase recall and false positives; higher thresholds usually increase precision while missing more positives. Prevalence changes predictive values even when sensitivity and specificity stay fixed.',
    worked:'Fraud prevalence is 1%. A rule with 90% recall and 95% specificity flags 1,395 of 10,000 transactions: 90 true frauds and 1,305 false alarms. Precision is only 6.5%, so investigation capacity matters.',
    interactive:'Adjust prevalence, threshold and false-negative cost. Watch precision, recall and expected cost respond.', assumptions:'Evaluation prevalence and case mix must match intended use. Labels and error costs must be credible; grouped or time structure must be preserved.', failures:'Accuracy and ROC-AUC can look healthy while positive predictions are unusable. Oversampling outside folds leaks, and resampling changes apparent prevalence and calibration.',
    workflow:['Define positive class and action','Estimate realistic prevalence and costs','Use stratified plus group/time-aware validation','Resample or weight inside training folds','Calibrate probabilities when needed','Choose threshold on validation data','Evaluate once on test data'], report:'Report prevalence, confusion counts, precision, recall, specificity, PR-AUC, calibration, threshold, cost assumptions and uncertainty.', misuse:'Balancing the test set and then reporting its precision as deployment precision.',
    questions:[['A rare positive class makes accuracy…',['Potentially misleading','A calibration metric','Always below 50%'],0,'Predicting the majority class can dominate accuracy.'],['Oversampling belongs…',['Inside each training fold','Before cross-validation','In the final test set'],0,'Held-out rows must remain untouched and at natural prevalence.'],['Which curve often reveals rare-positive performance?',['Precision–recall','Only residual Q–Q','Scree plot'],0,'PR curves focus on positive retrieval and predictive value.'],['Threshold choice should use…',['Validation data and explicit costs','The final test set repeatedly','0.5 by law'],0,'Thresholds are decision hyperparameters.'],['After case-control sampling, probabilities may need…',['Recalibration to deployment prevalence','A larger p-value','PCA automatically'],0,'Sampling changes the class prior seen during training.']], interaction:'imbalance'
  },
  'experimental-design': {
    title:'Experimental Design & Randomisation', badge:'Statistics · Study Design', intro:'Build causal credibility before collecting data.', outcomes:['Define estimands','Use randomisation','Plan power and analysis'], prerequisite:'Probability & Sampling Distributions', next:'ANOVA or Mixed Models',
    problem:'Causal conclusions depend more on assignment, measurement and follow-up than on a sophisticated final test. Design defines what comparison the data can identify.', intuition:'Random assignment balances confounders in expectation. Blocking improves balance on known prognostic variables; clustering changes the independent unit; blinding protects measurement and behaviour.',
    worked:'Twenty schools—not 600 students—are randomised to a teaching programme. Student outcomes remain clustered within schools. Treating 600 rows as independent exaggerates precision; analysis and power must account for 20 randomisation units and intraclass correlation.',
    interactive:'Adjust sample size, clustering and effect size. Compare naive and design-adjusted effective sample size and approximate power.', assumptions:'Assignment is implemented as planned, outcomes are measured consistently, interference is limited or modelled, and attrition does not destroy exchangeability.', failures:'Noncompliance, differential attrition, spillovers, outcome switching, hidden multiplicity and analysing below the randomisation unit can bias results or uncertainty.',
    workflow:['Define population, intervention, comparator, outcome and estimand','Choose the randomisation unit','Block or stratify when justified','Plan sample size with clustering and attrition','Pre-specify primary analysis and multiplicity','Track adherence and missingness','Report intention-to-treat and justified secondary analyses'], report:'Report protocol, randomisation and concealment, units, exclusions, attrition, adherence, effect with confidence interval, harms, multiplicity and deviations.', misuse:'Randomising classrooms but analysing pupils as though all observations were independent.',
    questions:[['Schools assigned to treatment means the randomisation unit is…',['School','Student','Outcome measurement'],0,'Assignment occurred at the school level.'],['Clustering usually makes naive standard errors…',['Too small','Exactly correct','Always infinite'],0,'Within-cluster similarity reduces effective independent information.'],['The estimand specifies…',['The population-level causal contrast of interest','Only the p-value threshold','The chart colour'],0,'It defines precisely what effect the study aims to estimate.'],['Blocking is used to…',['Improve balance and precision on known factors','Create confounding','Replace randomisation'],0,'Randomisation still occurs within planned blocks.'],['Twenty outcomes tested without a plan create…',['Multiplicity risk','Guaranteed power','No false positives'],0,'More tests create more chances for a small p-value.']], interaction:'design'
  }
};

const reasoningFrame = 'A defensible analysis connects the mathematical target to the real decision. Identify what is estimated, which observations contribute independent information, what choices are learned from data, and which uncertainty remains after fitting. Then ask how the conclusion would change under another reasonable sample, a plausible alternative specification, or a shift in the deployment population. This reasoning must happen before the final test set is opened. The test result evaluates the locked procedure; it does not choose that procedure. Sensitivity analysis is most useful when it is planned around credible failure modes rather than used to search for a preferred answer.';
const deepNotes = {
  'evaluation-metrics':'MAE targets absolute error in outcome units, while squared-error optimisation gives large misses extra weight. Accuracy summarises a thresholded confusion matrix; precision, recall and specificity condition on different denominators. ROC-AUC measures ranking across thresholds, whereas PR-AUC emphasises positive retrieval and depends on prevalence. Log loss and Brier score evaluate probabilities, and calibration asks whether predicted risks match observed frequencies. Out-of-sample R² compares with a mean-prediction baseline and may be negative. With asymmetric consequences, write expected cost explicitly and choose the threshold that supports the actual decision and operational capacity.',
  'data-leakage-pipelines':'Leakage can enter through features, rows, groups, time or repeated comparison. A post-outcome billing code is feature leakage; duplicate images across folds are row leakage; tomorrow’s rolling average is time leakage; repeatedly checking the same test score creates selection leakage. Nested cross-validation separates inner model selection from outer evaluation. Target encoding, oversampling, outlier rules, feature selection, PCA and threshold selection are all estimators. Each must be re-learned within every training fold. The decisive audit question is whether a value, rule or analyst decision could have known anything about the held-out outcome.',
  'regression-diagnostics':'Residual-versus-fitted plots examine mean form and variance structure. Q–Q plots compare ordered residuals with a reference distribution and matter for inference that relies on that reference; predictors and outcomes need not themselves be normal. Leverage describes unusual predictor combinations. Influence describes how fitted results change, so Cook’s distance is a screening tool rather than a deletion command. VIF shows how strongly a predictor overlaps the others and can explain wide coefficient intervals without proving bias. Diagnostics should prompt a scientific response: correct errors, represent nonlinearity, model variance or dependence, and report justified sensitivity analyses.',
  'probability-sampling':'Discrete probability masses add directly, whereas continuous density integrates to probability over an interval. Density height is not itself point probability and may exceed one. A statistic is a random variable before data are observed, and its sampling distribution depends on the population, statistic and design. The central limit theorem supports many normal approximations but does not make every small or heavy-tailed sample normal. Standard deviation describes individual variation; standard error describes estimator variation. Clustering, unequal weights and finite-population sampling change the familiar independent-sample formula and the effective amount of information.',
  'confidence-hypothesis-testing':'A p-value is a tail probability calculated under H₀ and every model assumption; the null is conditioned on, not assigned a probability. A compatible confidence interval often inverts a family of tests. Sample size affects precision, so tiny effects can be statistically clear and important effects can remain uncertain. Power is planned against a specific meaningful alternative and is not the probability that an observed finding is true. Family-wise and false-discovery-rate procedures answer different multiplicity goals. Neither repairs optional stopping, outcome switching or selective reporting after results are known.',
  'random-forest':'Bootstrap samples vary observations and random feature subsets vary candidate splits. Diversity matters because averaging highly correlated trees removes less variance. Out-of-bag predictions provide a development estimate, but extensive optimisation against that estimate still needs outer evaluation. More trees reduce Monte Carlo noise; they do not cure leakage, biased sampling or drift. Permutation importance measures predictive reliance under disruption, not causality. Correlated predictors can substitute for one another, and partial dependence can average over unrealistic combinations. Leaf size and weighting also affect the smoothness and calibration of predicted probabilities.',
  'gradient-boosting':'Boosting creates an additive predictor by fitting each learner to a negative loss gradient. For squared error this resembles residual fitting, but other losses create different pseudo-residuals. Learning rate scales contributions, tree depth limits interactions, and row or feature subsampling regularises. These settings interact, so learning rate and round count must be tuned together. Training loss normally keeps falling and cannot select stopping. Validation-based early stopping is a hyperparameter choice. Class weighting changes the objective and can damage calibration. Gain importance and attribution methods explain the fitted model, not a causal mechanism.',
  'missing-data-encoding':'MCAR, MAR and MNAR are assumptions about the missingness process, not labels that a plot can prove. Complete-case analysis is valid for limited targets under restrictive conditions. Single imputation treats filled values as known and usually understates inferential uncertainty; multiple imputation combines within- and between-imputation variation. Encoding defines geometry: one-hot separates unordered levels, ordinal encoding asserts order, and target encoding borrows outcome information. Target and frequency encoders need fold-safe fitting and an unseen-level policy. Raw columns and a data dictionary should be retained so every transformation remains auditable.',
  'imbalanced-classification':'Sensitivity and specificity condition on actual class, while precision conditions on predicted positives; therefore precision changes strongly with prevalence. A small false-positive rate can still create an overwhelming number of alerts when negatives dominate. ROC and precision–recall curves show different trade-offs and neither chooses an operational threshold automatically. Weighting changes the training objective; resampling changes the examples seen. Synthetic cases must remain inside training folds and may be implausible. Calibration may need correction after altered sampling or prior shift. Confusion counts reveal workload that rates alone can hide.',
  'experimental-design':'An estimand specifies population, treatment strategies, comparator, outcome, time and summary contrast. Allocation concealment protects enrolment; blinding protects behaviour and measurement. Blocking randomises within prognostic strata. Cluster randomisation assigns groups and reduces effective sample size through intraclass correlation, so power and analysis must use the randomisation unit. Intention-to-treat preserves the randomised contrast; per-protocol analysis answers a different, more selection-sensitive question. Noncompliance, attrition and interference should be anticipated in the protocol. Power planning must include meaningful effect, variance, allocation, clustering, multiplicity and attrition.'
};

const esc = s => String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
function quizHtml(questions){return questions.map((q,i)=>`<div class="question" data-question="${i+1}"><fieldset><legend>${esc(q[0])}</legend><div class="quiz-options">${q[1].map((a,j)=>`<button class="quiz-option" type="button" data-correct="${j===q[2]}" data-feedback="${esc(q[3])}">${esc(a)}</button>`).join('')}</div><p class="feedback" aria-live="polite"></p><details class="static-answer"><summary>Answer and explanation</summary><p><strong>${esc(q[1][q[2]])}.</strong> ${esc(q[3])}</p></details></fieldset></div>`).join('')}
function page(slug,d){const stages=[['The problem',d.problem],['Intuition and objective',d.intuition],['Worked example',d.worked],['Reasoning with the method',`<p>${deepNotes[slug]}</p><p>${reasoningFrame}</p>`],['Interactive laboratory',`<p>${d.interactive}</p><div class="interactive" data-interactive="${d.interaction}"><canvas class="viz" width="620" height="300" aria-label="${esc(d.title)} interactive visualisation"></canvas><div class="interactive-controls"></div></div>`],['Assumptions and failure modes',`<h3>Assumptions</h3><p>${d.assumptions}</p><h3>When the method fails</h3><p>${d.failures}</p>`],['Practical workflow',`<ol class="workflow">${d.workflow.map(x=>`<li>${x}</li>`).join('')}</ol>`],['Interpretation and reporting',`<p class="callout report"><strong>What to report:</strong> ${d.report}</p><p class="callout"><strong>Common misuse:</strong> ${d.misuse}</p>`],['Knowledge checks',`<div class="knowledge-check" data-knowledge-check>${quizHtml(d.questions)}</div>`]];return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="description" content="${esc(d.intro)}"><link rel="stylesheet" href="foundation-module.css"><link rel="stylesheet" href="handwritten-theme.css?v=20260826-handwritten-final-2"><script src="handwritten-theme.js?v=20260826-handwritten-final-2" defer></script><script src="foundation-module.js?v=20260826-foundation-final-2" defer></script><title>${esc(d.title)} · StatML Academy</title></head><body><header class="hero"><a class="home" href="../index.html">← StatML Academy</a><div class="eyebrow">${esc(d.badge)}</div><h1>${esc(d.title)}</h1><p>${esc(d.intro)}</p><div class="outcomes">${d.outcomes.map(x=>`<span>${esc(x)}</span>`).join('')}</div><div class="pathway"><div><small>Prerequisites</small>${esc(d.prerequisite)}</div><div><small>Recommended next</small>${esc(d.next)}</div></div></header><main class="shell"><article class="lesson">${stages.map((s,i)=>`<section class="stage" id="stage-${i+1}" data-stage="${i+1}"><p class="stage-label">Stage ${String(i+1).padStart(2,'0')}</p><h2>${s[0]}</h2><div>${s[1]}</div></section>`).join('')}</article><nav class="map" aria-label="Lesson stages"><strong>Lesson map</strong>${stages.map((s,i)=>`<a href="#stage-${i+1}">${String(i+1).padStart(2,'0')} · ${s[0]}</a>`).join('')}</nav></main></body></html>`}

const root=resolve(import.meta.dirname,'..');
for(const [slug,data] of Object.entries(lessons)) await writeFile(join(root,'modules',`${slug}.html`),page(slug,data));
export { lessons };
